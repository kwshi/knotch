#!/usr/bin/env python3
from sys import argv
import knotch

# Intro text
print('Kye\'s Number to Text Changer (knotch)')

del argv[0]
padding = len(max(argv, key=len))
for arg in argv:
	print('%s knotches to: %s' % (arg.ljust(padding), knots.knot(int(arg))))
